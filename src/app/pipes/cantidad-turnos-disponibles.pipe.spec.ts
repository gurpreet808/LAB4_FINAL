import { CantidadTurnosDisponiblesPipe } from './cantidad-turnos-disponibles.pipe';

describe('CantidadTurnosDisponiblesPipe', () => {
  it('create an instance', () => {
    const pipe = new CantidadTurnosDisponiblesPipe();
    expect(pipe).toBeTruthy();
  });
});
