import { TurnoFormMinutosPipe } from './turno-form-minutos.pipe';

describe('TurnoFormMinutosPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnoFormMinutosPipe();
    expect(pipe).toBeTruthy();
  });
});
